{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "AeroPulse-NG operational capability set: window management for the dual-display workspace and event channels for the telemetry bus.",
  "windows": ["radar_main", "ops_hud"],
  "permissions": [
    "core:default",
    "core:window:allow-create",
    "core:window:allow-close",
    "core:window:allow-set-title",
    "core:window:allow-set-fullscreen",
    "core:event:default"
  ]
}
