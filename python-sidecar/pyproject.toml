[project]
name = "aeropulse-ng-sidecar"
version = "2.4.0"
description = "DuckDB persistence + AWOS serial ingestion sidecar for AeroPulse-NG"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
